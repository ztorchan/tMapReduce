syntax = "proto3";

package mapreduce;

option cc_generic_services = true;

enum WorkerState {
  UNKNOWN = 0;
  INIT = 1;
  IDLE = 2;
  WAIT2MAP = 3;
  MAPPING = 4;
  WAIT2REDUCE = 5;
  REDUCING = 6;
  CLOSE = 7;
}